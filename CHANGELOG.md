# 更新日志 · 魔法粒子 Magic Particle

记录每次有实质改动的版本。格式参考 [Keep a Changelog](https://keepachangelog.com/)。

> 基线说明：以下条目为 README「本次更新内容」之后（约 2026-08-15 之后）的累计改动。

---

## [未发布] — 2026-08-16

### ✨ 新增
- **关于页面独立化**：「关于」从首页底部可展开行，升级为独立页面 `about.html`。首页顶部导航栏「关于」按钮改为 `<a href="about.html">` 跳转，不再滚动当前屏；`about.html` 自带终端窗口、站点简介、`asiawest.pages.dev` 主页链接、GitHub 卡片（头像自动拉取 + 名称 + 简介 + 主页链接）、右上角主题切换、左上角「← 返回」链回首页。
- **开源仓库地址**：在 `about.html` 的 GitHub 卡片内补上真实仓库链接 `https://github.com/Artruok/-MagicParticle`（注意仓库名带前导短横线）。

### 🎨 界面 / 交互
- **「关于」入口上移并常驻**：先是把关于入口从底部高亮按钮挪到顶部导航栏（月亮按钮左侧）做常驻高亮，随后去掉了首页底部那一行横条，只保留顶部入口。
- **生成按钮 `$` 配色区分**：图片模式的「开始生成」按钮上 `$` 改为白色；文字模式的「用文字生成」、词云模式的「用词云生成」按钮上 `$` 改为黑色（深色/浅色主题下都清晰）。
- **生成按钮位置调整**：「用文字生成」「用词云生成」从各自面板中部移出，合并为导入窗口**最底部**的共享块（`#genActions`），位于「字体」「厚度」选项下方，真正贴底、不再藏在中间。

### 🐛 修复
- **厚度滑块不显示的隐藏 bug**：`app.js` 的 `setMode` 中长期引用了一个从未声明的变量 `extrudeRow`，导致每次切换「文字 / 词云」标签都会抛错、后续逻辑中断——文字/词云的厚度（3D 立体挤出）滑块因此**一直无法正常出现**。已补上声明，厚度滑块恢复正常。

---

## 更早的累计改动（README 中「本次更新内容」）
- 项目改名 `particle-forge` → **魔法粒子 Magic Particle**
- 关于页重构（底部可展开行）
- 顶部进度条可点击退回（`$ 定义 / $ 精度 / $ 预览`）
- 深度图选项移至精度步骤（导入后依旧可见）
- 新增 AI 提示词框（深度图下方，可复制 / 随机示例）
- 词云 Python wordcloud 同款算法、文字/词云厚度 3D、精度超高档/极限档/自定义、渲染 FPS、旋转/3D 照片拉回速度、鼠标排斥独立面板、词云多套配色、3D 照片倾斜 + 真实深度图接入、全窗口拖拽、Terminal UI 风格、全中文界面
